server:
  # Основной порт делаем защищенным (для WSS)
  port: 8443
  ssl:
    enabled: true
    key-store-type: PKCS12
    key-store: file:/app/keystore.p12
    key-store-password: password
    key-alias: springboot
